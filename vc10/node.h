/*#include "../vc10/Circle.h"

class node{
public:
	node();
	node(Circle* cir, node *node);

	node* next_;
	node* prev_;
	Circle* dataCircle_;

	void insertAfter(Circle* new_data, Circle* after_cir);
	void insertBefore(Circle* new_data, Circle* before_cir);
	void update();
	//void draw(Vec2i pointer);
};*/